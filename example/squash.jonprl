Operator squash : (0).
[squash(A)] =def= [subset(unit; _. A)].

Theorem squash-wf : [∀(U{i}; A. ∈(squash(A); U{i}))] {
  unfold <squash>; auto.
}.

Theorem squash-intro : [∀(U{i}; A. Π(A; M. squash(A)))] {
  unfold <squash>; auto.
}.
